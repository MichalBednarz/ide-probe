include required(classpath("ideprobe.conf"))

probe {
  workspace.path = "https://github.com/sbt/io"
}

modules {
    verify = ["argonaout", "async-http-client", "bench", "blaze-client"]
    test = ["IO"]
}