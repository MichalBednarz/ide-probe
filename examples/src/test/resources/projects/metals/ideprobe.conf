include required(classpath("ideprobe.conf"))

probe {
  workspace.path = "https://github.com/scalameta/metals"
}

test {
  modules = ["examples-sources"]
}

modules {
    verify = ["core-sources", "coreJS", "scratch-sources", "scratchJS"]
    test = ["mtest", "unit"]
}